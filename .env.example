# Local development settings. Copy to .env for native setup; never commit .env.
# Database and secret placeholders owned by later tasks are added by those tasks.

# Local SQLite database. Absolute paths resolve identically for the API, worker,
# migrations, and commands regardless of working directory. When unset, the app
# uses ./data/math_tutor.sqlite3. Only sqlite: URLs are accepted (see D004).
#DATABASE_URL=sqlite+pysqlite:///data/math_tutor.sqlite3

# Optional private data directory used when DATABASE_URL is unset or relative.
#MATH_TUTOR_DATA_DIR=./data

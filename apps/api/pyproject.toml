[project]
name = "math-practice-tutor"
version = "0.1.0"
description = "Backend for the self-hosted Math Practice Tutor"
requires-python = ">=3.14.7,<3.15"
dependencies = [
    "fastapi>=0.141.1,<1",
    "pydantic>=2.13.5,<3",
    "uvicorn>=0.52.4,<1",
]

[dependency-groups]
dev = [
    "httpx2>=2.12,<3",
    "mypy>=2.3.1,<3",
    "pre-commit>=4.6.2,<5",
    "pre-commit-hooks>=6.0.0,<7",
    "pytest>=9.1.1,<10",
    "ruff>=0.16.6,<1",
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13"]
build-backend = "uv_build"

[tool.uv]
required-version = ">=0.12.10,<0.13"

[tool.uv.build-backend]
module-name = "math_tutor"

[tool.pytest.ini_options]
addopts = ["-ra", "--strict-config", "--strict-markers"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
target-version = "py314"

[tool.ruff.lint]
select = ["B", "E4", "E7", "E9", "F", "I", "SIM", "UP"]

[tool.mypy]
python_version = "3.14"
strict = true
warn_unreachable = true
